import 'package:flutter/material.dart';

class PayCompleteScreen extends StatelessWidget {
  static String path = '/pay/complet';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}
