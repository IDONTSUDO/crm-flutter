enum AppState { initial, authenticated, authenticating, unauthenticated }
enum ComerceOperation { LOG_PASS, TRANSFER, GROUP, YOUR_PAY }
enum InputPay { getPay, atPay }
enum PayType { QIWI }
enum GroupPaymentSteps { INIT, CHECK_USER_AT_GROUP, FINAL }
